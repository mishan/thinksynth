# $Id: Makefile.in,v 1.21 2004/02/04 21:08:39 misha Exp $

BINARY = thinksynth

bindir=@bindir@
prefix=@prefix@

all: thinksynth

include ../build.mk

PICFLAGS = -fPIC
INCLUDES = -I.. -I../libthink
LDFLAGS += @LIBS@ -L../libthink -lthink

MAINOBJS = main.o
#SEQOBJS = seq.o

#thinkseq: $(SEQOBJS)
#	$(CXX) $(CXXFLAGS) -o $@ $(SEQOBJS) $(LDFLAGS) -lthink -L.

thinksynth: $(MAINOBJS)
	$(CXX) $(CXXFLAGS) -o $@ $(MAINOBJS) $(LDFLAGS) -lthink -L.

clean::
	rm -f thinksynth

distclean::
	rm -f Makefile

install:
	mkdir -p $(DESTDIR)$(bindir)
	cp -f thinksynth $(DESTDIR)$(bindir)
#	cp -f thinkseq $(DESTDIR)$(bindir)

uninstall:
	rm -f $(DESTDIR)$(bindir)/thinksynth
#	rm -f $(DESTDIR)$(bindir)/thinkseq
